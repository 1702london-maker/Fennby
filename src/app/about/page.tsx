import { SimplePage } from "@/components/SimplePage";

export default function AboutPage() {
  return (
    <SimplePage
      eyebrow="Company"
      title="About Fennby"
      body="Fennby is a whole-child education ecosystem, starting with 11+ tutoring and expanding into vocational training, mentorship, and school partnerships — built from day one on one promise: every child is seen, every step is visible, and every adult around them is accountable."
    />
  );
}
