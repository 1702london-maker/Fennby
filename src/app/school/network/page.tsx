import { SimplePage } from "@/components/SimplePage";

export default function SchoolNetworkPage() {
  return (
    <SimplePage
      eyebrow="For Schools"
      title="Inter-school network"
      body="Friendly, structured quizzes and competitions between partner schools — with anonymised, school-level leaderboards that make the whole network stronger."
    />
  );
}
